<%= form_with model: @task do |f| %>
  <p>Edit task: </p>

  <div>
   <%= f.label :name %>
   <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <%= f.submit "Update task", class: 'update-task-button' %>

<% end %>