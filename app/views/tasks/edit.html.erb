<%= form_with model: @task, class: 'edit_task' do |f| %>
<p>Please provide edited task information for our database:</p>
 <div>
        <%= f.label :name %>    <%#How do I pre fill the form? is it with the model above? %>
        <%= f.text_field :name %>
    </div>
      <div>
        <%= f.label :description %>
        <%= f.text_area :description %>
    </div>
      <div>
        <%= f.label :completed_at %>
        <%= f.text_field :completed_at %>
    </div>
    <%= f.submit "Edit a task!"%>
<% end %>