<h1>Edit Task</h1><%# form_with takes a hash - model - and then creates a loop %>
<%= form_with model: @task, class: "edit-task" do |f| %>
<p>Please provide the following information to edit your task in the database:</p>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.label :completed_at %>
  <%= f.text_field :completed_at %>

  <%= f.submit "Edit Task", class: "task-button" %>


<% end %>