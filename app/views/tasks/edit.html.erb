<h3>New Task<h3>
<%= form_with model: @task, class: 'edit-task' do |f| %>
<p>Enter the information to edit a task</p>
<%= f.label :name %>
<%= f.text_field :name%>
<%= f.label :description %>
<%= f.text_area :description %>
<%= f.submit "Save edited task", class: "button" %>
<% end %>