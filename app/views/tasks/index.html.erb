<body class="wholelist">
<h2 class="title">Let's Get This Done:</h2>

  <% if @tasks.length > 0 %>
    <ul class="tasklist">
      <% @tasks.each do |task|%>
        <li class="task_indiv">
          <%= task[:name] %> : <%= task[:description] %>
          <% if task[:completed_at] == 'completed' %>
            <h3 class="completed">Task Completed</h3>
          <% end %>
          <div class="task_buttons">
            <%= button_to "Show Details", task_path(task.id), :method => :get%> 
            <%= button_to "Edit Task", edit_task_path(task.id), :method => :get%>
            <%= button_to "Delete Task", delete_path(task.id), :method => :delete%>
            <% if task[:completed_at] != 'completed'%> 
              <%= button_to "Mark Task Complete", completed_path(task.id), :method => :get%>
            <% end %> 
          </div>
        </li>
      <% end %>
    </ul>
  <% else %>
    <h3>No tasks in the system!</h3>
  <% end %>

</body>