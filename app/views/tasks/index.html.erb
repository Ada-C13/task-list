<h2>Tasks for Today<h2>

<ul>
  <% @tasks.each do |task| %>
    <li class="task">
      <%= task[:name] %>
      <p><%= task[:description] %></p>
      <p><%= task[:completed_at] %></p>
    </li>
  <% end %>
</ul>