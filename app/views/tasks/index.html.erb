<h1>Task List</h1>
<% if @tasks.length > 0 %>
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= task.name %>
        -
        <%= task.description %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h3>No tasks in the system!</h3>
<% end %>