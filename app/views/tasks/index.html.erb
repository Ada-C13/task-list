<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task|  %>
    <li>
      <%= task.name %> <%= task.description %> <%= task.completed_at %> <%= link_to "Edit Task", edit_task_path(task.id) %>
    </li>
  <% end %>
</ul>
<%= link_to "Add new task", new_task_path %>