<h2>Tasks for today</h2>

<% if @tasks.length > 0 %>
  <ul>
    <% @tasks.each do |task| %>
      <li> <strong>Task:</strong> <%= task.name%> <strong>Description:</strong> <%= task.description%> <strong>Completed:</strong> <%= task.completed_at%></li>
    <% end %>
  </ul>
<% else %>
  <h3>No tasks for today!</h3>
<% end %>