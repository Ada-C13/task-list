<h1>Tasks</h1>
<button><%= link_to "New task", new_task_path %></button>
<section class="tasks">
  <% @tasks.each do |task|  %>
    <div class="task">
    <%= link_to "#{task.id}. #{task.name}", task_path(task) %>
    </div>
  <% end %>
</section>