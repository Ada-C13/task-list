<% if @tasks.length > 0 %>
  <ol>
    <% @tasks.each do |task| %>
      <li><%= task[:name] %></li>
      <ul>
        <li><%= task[:description] %></li>
        <li><%= task[:completed_at] %></li>
      </ul>
    <% end %>
  </ol>
<% else %>
  <h3>No tasks in the system!</h3>
<% end %>