<h1>Tasks#index</h1>
<p>Find me in app/views/tasks.index.html.erb</p>

<h1>Tasks</h1>
<% if @tasks.length > 0 %>
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= task %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h3>No tasks in the system!</h3>
<% end %>