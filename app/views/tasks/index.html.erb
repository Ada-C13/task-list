<h1>Tasks</h1>

<% if @tasks.length > 0 %>
  <ul>
    <% @tasks.each do |task|  %>
      <li>
        <%= link_to task.name, task_path(task.id)%> -due date: <%= task.completed_at %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h3>There are no tasks</h3>
<% end %>