<h1>Tasks List</h1>

<% if @tasks.length > 0 %>
  <ul>
    <% @tasks.each do |task|%>
      <li>
      <%= link_to task.name, tasks_path(task)%>
      Description: <%= task.description %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h3>No tasks in the system!</h3>
<% end %>