<h1>YOUR TASKS</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
    <%= link_to task.name, task_path(task.id) %> |
    <%= link_to "Edit", edit_task_path(task.id) %> |
    <%= link_to "Delete", task_path(task.id), method: :delete, data: {confirm: "You sure you want to delete?"} %>
    </li>
  <% end %>
</ul>
