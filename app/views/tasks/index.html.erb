<h1>Tasks</h1>
<section class="tasks">
  <% @tasks.each do |task|  %>
    <div class="task">
    <h4><%= task.id %>. <%= task.name %></h3>
    <p><%= task.description%></p>
    <p><%= task.completed_at %></p>
    </div>
  <% end %>
</section>