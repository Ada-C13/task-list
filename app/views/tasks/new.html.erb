<h3>New Task<h3>
<%= form_with model: @task, class: 'create-task' do |f| %>
<p>Enter your new task:</p>
<%= f.label :name %>
<%= f.text_field :name%>
<%= f.label :description %>
<%= f.text_area :description %>
<%= f.submit "Save task", class: "button" %>
<% end %>