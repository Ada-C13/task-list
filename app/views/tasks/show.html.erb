<h3><%= @task.name %></h3>
<h5><%= @task.description %> - Due date: <%= @task.completed_at %></h5>
<p><%= link_to "Edit task", edit_task_path %> | <%= link_to "Delete task", task_path(@task), method: :delete, data: {confirm: 'Are you sure you want to delete?'} %></p>
