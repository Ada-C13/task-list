<h2><%= @task[:name]%></h2>
<p><%= @task[:description]%></p>
<%= button_to "Edit Task", edit_task_path(@task.id), :method => :get%>
<%= button_to "Delete Task", delete_path(@task.id), :method => :delete, data: {confirm: "Are you sure you want to delete the task: #{@task.name}?"}%> 