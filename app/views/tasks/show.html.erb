<h2>Task </h2>
<p><%= @task.name %> : <%= @task.description %></p>

<%= link_to "Edit Task", edit_task_path %>

<%= link_to "Delete Task", task_path(@task.id), method: :delete , data: {confirm: "Are you sure?"}%>

<%= link_to "Mark Complete", task_path(@task.id), method: :patch %>