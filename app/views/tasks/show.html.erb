<h2>Task Name: <%=@task.name%> <em><%=link_to "(edit task)", edit_task_path(@task)%></em></h2>
<p><strong>Description:</strong> <%=@task.description%></p>
<p><strong><%=@task.completed_at ? "Completed at:" : ""%></strong>  <%=@task.completed_at%>  </p>
<em><%= link_to "Delete Task", task_path(@task), method: :delete, data: {confirm: "Are you sure?"}%></em>
