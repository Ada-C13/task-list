<div class="task">
  <h2><%= @task.id %>. <%= @task.name %></h2>
  <p><%= @task.description%></p>
  <p><%= @task.completed_at %></p>
</div>
<button><%= link_to "back to all tasks", tasks_path %></button>
